# 语音与表情机器人控制系统

## 系统介绍

本系统包含的功能包有：

语音识别功能包：VoiceOffline

自定义语音话题接口：voice_msgs

语音合成功能包：vits

关键词语音唤醒功能包：snowboyros

大语言模型功能包:llm(chatglm2),llm2_test(llama2)

表情机器人控制功能包：head-control

## 功能演示说明

### 1.启动大语言模型(llama2（演示用）/chatGLM2（本机调试）)

llama2

```
cd /media/ros/1T/sk/llama_ws
conda activate sk
source install/setup.bash
ros2 run llm2_test llm_test
```

chatGLM2

```
# 启动服务端
cd kaldi_vits_glm/src/llm/llm
conda activate chatGLM
python openai_api.py
# 另开一窗口，启动ros2节点
cd kaldi_vits_glm
source install/setup.bash
ros2 run llm ros_llm
```

### 2.启动语音系统

该launch文件已包含VoiceOffline、vits、snowboyros包内所需ros2节点的启动

```
cd kaldi_vits_glm
source install/setup.bash
ros2 launch VoiceOffline voice_function.launch.py
```

语音识别默认是使用语音唤醒作为条件触发的，如果想使用按键作为触发条件，则

```
cd kaldi_vits_glm
source install/setup.bash
ros2 run VoiceOffline ros_kaldi_keyword
```

### 3.启动表情控制系统

该launch文件包含facial_features(接收Servo信息并通过串口发送给电机控制板)、facial_expression(获取预设的xml表情文件，并检查订阅的表情动作是否有对应的xml文件，如果有则发送对应的动作Servo信息)、facial_mouth_audio(根据音频控制嘴部动作)、facial_eye_blink(随机眨眼)、facial_face_ctrl(随机动作)。

```
cd kaldi_vits_glm/src/head-control
source install/setup.bash
ros2 launch ulaahead all.launch.py
```

## 一些部署说明

### 关于llm包

使用llm包需要部署chatglm2

https://github.com/THUDM/ChatGLM2-6B

并将kaldi_vits_glm/src/llm/llm/openai_api.py中模型加载的地方换成自己的chatglm2模型位置，host改为自己的ip。将kaldi_vits_glm/src/llm/llm/api_use.py中的openai.api_base也换为自己的ip

### 关于vits的训练

参考https://www.bilibili.com/video/BV1Lo4y1B7JX/?spm_id_from=333.337.search-card.all.click&vd_source=423e954c731d7819e4e87619761c69cc

也可以把文件下载到本地训练。想用自己训练好的声音只需要把训练得到的模型和config文件替换调vits包内的文件即可。
