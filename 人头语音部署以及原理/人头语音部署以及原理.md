

## 演示
启动语音问答

```
source ./install/setup.bash 
ros2 run voiceinteraction  aiui_interaction
```
准备：插入话筒和麦克风




![输入图片说明](Screenshot%20from%202023-08-03%2016-51-23.png)

人头关于语音的主要是这三个文件，
    `ulaahead  ulaa_msgs  voiceinteraction`
文件可以在杨琦上传的文件中找到，网址 ：
    `https://gitee.com/usst-imi/head-control/tree/master/src`

ulaahead 主要是根据语音控制人头嘴部动作文件，ulaa_msgs主要是相关的消息，voiceinteraction主要问答部分


## 代码结构

###  aiui_interaction部分(语音交互)
```
ros2 run voiceinteraction  aiui_interaction
```
代码路径 src/voiceinteraction/src/aiui_interaction.cpp
![输入图片说明](Screenshot%20from%202023-08-03%2017-31-39.png)


功能：创建节点   ```/iat_publish```
    该节点相关话题

```
/iat_publish
  Subscribers:
    /parameter_events: rcl_interfaces/msg/ParameterEvent
    /speak_state: user_interfaces/msg/State
    /tts2speech_xfold: std_msgs/msg/String
    /voice_interaction: std_msgs/msg/String
  Publishers:
    /iat_answer_text: std_msgs/msg/String
    /iat_question_text: std_msgs/msg/String
    /parameter_events: rcl_interfaces/msg/ParameterEvent
    /rosout: rcl_interfaces/msg/Log
    /speak_pos: user_interfaces/msg/State
    /speech2tts_xfold: std_msgs/msg/String
    /tts2tts_xfold: std_msgs/msg/String
    /voice_angle: std_msgs/msg/UInt16
```


```
接收话题:
    /speak_state: user_interfaces/msg/State
    /tts2speech_xfold: std_msgs/msg/String
    /voice_interaction: std_msgs/msg/String
发布话题：
    /iat_answer_text: std_msgs/msg/String
    /iat_question_text: std_msgs/msg/String
    /speak_pos: user_interfaces/msg/State
    /speech2tts_xfold: std_msgs/msg/String
    /tts2tts_xfold: std_msgs/msg/String
    /voice_angle: std_msgs/msg/UInt16
```

代码结构


```

void aiui_interaction:: **createAgent** ()        //创建小贝   主要的id  密码等在此函数中
    appid    key
    读取配置文件，登陆&创建小贝

void aiui_interaction:: **setParams** ()  // 设置语义后合成时的发音人   小娟  小燕等不同声音
void aiui_interaction:: **agent_start** ()   // 发送消息  startMsg  启动交互
void aiui_interaction:: **agent_stop** ()  // 发送消息  stopMsg  停止交互
void aiui_interaction:: **app_destory** ()  // 停止录音
void aiui_interaction:: **stop_recorder** ()      // 关闭话筒
void aiui_interaction:: **recorder_creat** ()   //创建话筒
void aiui_interaction:: **recorder_start()**  // 启动录音
void aiui_interaction::readCmd()        // 接收用户输入命令，调用不同的测试接口   q-->**app_destory**      w   上一问题重答

回调
void aiuiflagCallback():  //  接收话题/voice_interaction
    话题内容是 "开始"    agent_start-->启动交互
    话题内容是 "停止"    agent_stop-->停止交互
void gttsflagCallback():   //  接收话题/tts2speech_xfold
    输出话题内容
void speakendCallback():   //  接收话题/speak_state
    暂时没用



 **int main：
    实例化节点 /iat_publish
    接收:
        /speak_state                //回调   speakendCallback
        /tts2speech_xfold           //回调    gttsflagCallback
        /voice_interaction          //回调    aiuiflagCallback
    发布：
        /iat_answer_text
        /iat_question_text
        /speak_pos
        /speech2tts_xfold
        /tts2tts_xfold
        /voice_angle
    设置配置文件路径
    GenerateMACAddress(mac)   // mac   为本机唯一ID
    AIUISetting::setSystemInfo(AIUI_KEY_SERIAL_NUM, mac);  // 设备设置对应唯一的SN  AIUI_KEY_SERIAL_NUM
    aiui_copy->createAgent();  //创建AIUI 代理，通过AIUI代理与SDK发送消息通信
    aiui_copy->recorder_creat();   // 创建麦克风
    set_awake_word(awake_words);  // 设置唤醒词
    aiui_copy->recorder_start();   // 满足条件时，启动录音** 


static void GenerateMACAddress(char *mac)
    获取本机的MAC地址。它通过使用socket和ioctl函数与系统接口交互，以获取网络接口的信息，提取MAC地址。

void gWakeup()    // 唤醒
void gSleep()     //  休眠
void gTTS(string text)  // 上传消息   这个很关键，里面填什么内容，就会播放什么音频
void clearAudioFile(char *fileName)   // 写入文件





```
















### 旧版配置

#### 配置1
适用于：x86 老版圆形麦克风

麦克风配置

若为初次使用，且未配置过本麦克风的 udev 规则，则执行如下指令进行配置，若已配置过，可忽略该步骤。注：xf_mic.rules 位于根目录。

sudo cp xf_mic.rules /etc/udev/rules.d/ #配置过本麦克风的 udev 规则

sudo service udev restart #重启udev服务命令\或者插拔麦克风设备

lsusb #测到设备:检测到 VID：PID 为 10d6：b003 的设备，则设备读取成功。


安装必要的声卡库：
sudo apt-get install libasound2-dev

安装必要的音频播放库：

```
sudo apt-get install sox

sudo apt-get install mplayer

sudo apt-get install sox libsox-fmt-all

sudo apt install pavucontrol
```


安装播放插件:

```
sudo apt-get install ffmpeg    # 使用指令ffplay -ar 44100 -ac 1 -f s16le -i ./201904091310_test.pcm

sudo apt-get install sox # 播放mp3

sudo apt-get install sox libsox-fmt-all

play *mp3
```


//在libs文件下 - /src/voiceinteraction/libs/x64     libmsc.so      //  记得打开兜底模式
sudo cp lib* /usr/lib

//在libs文件下 - /src/aiui_readme libhid_lib.so
sudo cp lib* /usr/lib

安装cJSON

```shell
cd aiui_readme
sudo rm -r cJSON
apt  install cmake 
sudo apt-get install rar
sudo apt-get install unrar
sudo unrar x cJSON.rar
cd cJSON
sudo mkdir build
cd build/
sudo cmake ..
sudo make
sudo make install 
```


到工作空间colcon build  三次


#### 配置2
编译流程
- 在home目录下新建文件夹humanoid_ws
- 解压文件到humanoid_ws
- 将 src/awake.pcm 和 awake.wav 复制到 src 同级目录下
- 查看创建 -> 帐号流程:修改appid和对应lib*文件
- 安装依赖
	- sudo apt-get install libasound2-dev
	- sudo apt-get install mplayer
	- pip install pyaudio
	- pip install tqdm
	- sudo apt-get install pulseaudio 
	- sudo apt-get install pavucontrol
- 进行编译
	- cd humanoid_ws
	- colcon build 
	- source install/local_setup.bash
- 单个节点测试
	- ros2 run humanoid_voice awaken_record # 麦克风录制音频节点
		- 打开 src同级目录下.wav文件 播放是否有声音
		- 若没有: (运行代码的状态下)打开pulseaudio ->Recording -> 切换读取设备
	- ros2 run humanoid_voice humanoid_voice # 命令解析
	- ros2 run humanoid_voice awaken_node # 启动讯飞语音唤醒节点
	
- launch 测试
	-  ros2 launch humanoid_voice humanoid_voice_control_launch.py 
- 报错解决:
	- QIVWSessionBegin failed! error code:10102

--------------------------------------------------
创建帐号      注意打开兜底模式
- https://aiui.xfyun.cn/ ## 帐号注册,应用创建
	- 在网页右上角(帐号位置)->进行实名制用于后续sdk 下载
	- 我的应用 -> 进入应用 -> 新建应用 -> 应用信息 appid  
	- humanoid_voice_base/src/humanoid_voice/awaken_node.py -> 230行 替换 appid 
	- humanoid_voice_base/src/humanoid_voice/iat_publish.py -> 161行 替换 appid
	- humanoid_voice_base/src/humanoid_voice/tts_subscribe.py -> 231行 替换 appid
- https://www.xfyun.cn/ ## 唤醒词语设置,sdk下载,对应文件替换
	- 点击控制台 -> 选择应用:自己创建的 -> 语音识别:语音唤醒 -> 唤醒此设置:小贝小贝 -> 提交 -> 语音唤醒SDK: Linux MSC 下载
	-> 选择平台:Linux -> AI能力:语音唤醒 -> 下载解压
![输入图片说明](Screenshot%20from%202023-08-06%2017-02-56.png)
替换 lib 文件
```
解压文件/libs/x64/libmsc.so 和 libw_ivw.so
humanoid_voice_base/libs/x64/libmsc.so 和 libw_ivw.so-> 替换          
将下载的sdk文件的bin下面的msc全体文件粘贴替换 src/humanoid_voice_base/msc
当前目录打开 terminal
sudo cp lib* /usr/lib
运行命令      sudo ldconfig
```
















## 常见error

### 1.麦克风
```
>>>>>无法打开麦克风设备，设备被占用
>>>>>成功打开麦克风设备
dev is NULL
```
![输入图片说明](Screenshot%20from%202023-08-03%2017-07-38.png)
```
lsusb
```
测到设备:检测到 VID：PID 为 10d6：b003 的设备，则设备读取成功。如果没有则检测听筒是否已经插入
![输入图片说明](2023-07-28%2019-55-40%20%E7%9A%84%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE.png)

接下来重启听筒
```
sudo service udev restart
```
重启udev服务命令并且插拔麦克风设备
编译前一定要运行   ```source install/setup.bash ```





## 语音交互节点使用说明

### 目录结构及文件

```
include：所需头文件路径
libs：
|-- x86
	|-- libmsc.so  // 32位动态库
|-- x64
	|-- libmsc.so  // 64位动态库
src:
|-- iat_publish.cpp   // 语音识别节点
|-- tts_subscribe.cpp // 文本转语音节点
|-- voice_control.cpp // 控制输出节点
.gitignore：git 忽略文件
CMakeLists.txt
package.xml
```

### 简易信息流描述

语音识别节点->控制输出节点->文本转语音节点

1. 语音识别节点，识别用户交互指令，发布指令文本信息；
2. 控制输出节点，订阅语音识别节点指令，发布cmd_vel以及执行结果文本信息；
3. 文本转语音节点，订阅控制输出节点的执行结果文本信息并合成语音输出；
4. 机器人控制节点，订阅控制输出节点的cmd_vel标准信息，执行前进、后退、左转、右转、停止等控制指令

## 编译测试方法

### 删除历史编译文件

rm -fr build/ install/ log/ msc/ tts_sample.wav

### 选择编译voiceinteraction包

colcon build --packages-select voiceinteraction

### 设置环境生效

cd /home/padroid/ros2_ws/
source install/setup.bash

### tts_subscribe节点测试消息

```
ros2 topic pub /tts_text std_msgs/String "data: '你好，我是机器人优拉'"
ros2 topic pub /tts_text std_msgs/String "data: '你好，我是机器人ulaa'"
ros2 topic pub /tts_text std_msgs/String "data: '前进'"
ros2 topic pub /tts_text std_msgs/String "data: '后退'"
ros2 topic pub /tts_text std_msgs/String "data: '左转'"
ros2 topic pub /tts_text std_msgs/String "data: '右转'"
ros2 topic pub /tts_text std_msgs/String "data: '停止'"
```

### 运行节点命令






