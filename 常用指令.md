
```shell
ssh ros@192.168.50.153      # droid     密码  123
```
```shell
ssh droid@192.168.50.160   # 12345
```

#### 调试指令

```shell
-i https://pypi.tuna.tsinghua.edu.cn/simple
```
```
-i https://pypi.mirrors.ustc.edu.cn/simple/
```

```shell
CUDA_VISIBLE_DEVICES=1 python3 your_script.py
```


安装.deb
```shell
sudo dpkg -i hello.deb
```
```shell
-c https://mirrors.tuna.tsinghua.edu.cn/anaconda/archive/
```
```shell
sudo chmod 777  /dev/ttyA*
```

```shell
ssh ros@192.168.50.202    # 123
kex_exchange_identification: Connection closed by remote host
# 解决办法
# 删除  /home/ubuntu/.ssh/config    文件
```
```shell
killall code
```
```shell
# 键盘失灵
ibus-daemon -r -d -x
ibus-daemon -drx
```
```shell
# 据历史命令前缀查找补全命令
vim ~/.bashrc
# 添加：
if [[ $- == *i* ]]
then
        bind '"\e[A": history-search-backward'
        bind '"\e[B": history-search-forward'
fi
# 最后：
source ~/.bashrc
# 试一下： 
# 在命令行中 输入mo然后按方向键向上就会自动补全以mo开头的历史命令，比如 mount
```
```shell
tensorboard --logdir logs/eye_cvae_servo_full_face653x635_resume/ --bind_all
```



```python
print("\033[31m"+ "输出红色字体" +"\033[0m")
```

```python
#!/usr/bin/python3
 
#类定义
class people():
    #定义基本属性
    name = ''
    age = 0
    #定义私有属性,私有属性在类外部无法直接进行访问
    __weight = 0
    #定义构造方法
    def __init__(self,n,a,w):
        self.name = n
        self.age = a
        self.__weight = w
    def speak(self):
        print("%s 说: 我 %d 岁。" %(self.name,self.age))
 
# 实例化类
p = people('runoob',10,30)
p.speak()
```



```shell
lcm-gen -p example_t.lcm
```




```shell
https://console.bce.baidu.com/qianfan/overview?_=1704358886907
用户名 imi-llm
密码 Imi123456
```

```shell
# 删除缓存
pip cache purge
```


查看

```shell 
echo $ROS_DOMAIN_ID    # 查看ROS_DOMAIN_ID
export ROS_DOMAIN_ID=<your_domain_id>  #  临时更改ROS_DOMAIN_ID
echo "export ROS_DOMAIN_ID=<your_domain_id>" >> ~/.bashrc    # 全局更改ROS_DOMAIN_ID
```

```shell
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
```
```
https://blog.csdn.net/Strive_For_Future/article/details/108006977
```
```shell
tmux new -s "session name"   # 创建session
```

```shell
tmux kill-session -t $session_name  # //删除指定session
```


ipynb转py文件
```shell
pip install --upgrade jupyter  # 下载jupyter
```
```shell
jupyter nbconvert --to script torchhub_inference_tutorial.ipynb   # torchhub_inference_tutorial.ipynb转py文件
```


```shell
# 感觉.zip占用空间比.tar.gz大
unzip FileName.zip          # 解压
zip FileName.zip DirName    # 将DirName本身压缩
zip -r FileName.zip DirName # 压缩，递归处理，将指定目录下的所有文件和子目录一并压缩
```

```
# 递归复制目录
cp -r /path/to/source_dir /path/to/destination/
```

#### 关于疼痛


```shell
conda activate /media/ros/1T001/Anaconda3/envs/env_biovid/  # 进入自建环境
cd /media/ros/1T001/yongtong/pain/demo_pain_1/PainAttnNet-main
```
![输入图片说明](%E5%85%B3%E4%BA%8E%E8%AE%BA%E6%96%87/%E5%85%B3%E4%BA%8E%E5%B0%8F%E8%AE%BA%E6%96%87/%E6%88%AA%E5%9B%BE%202023-11-20%2017-15-29.png)

#### 挂载硬盘

```shell
https://blog.csdn.net/zhangpeterx/article/details/83928344
```

```shell
Device         Start        End    Sectors   Size Type
/dev/nvme1n1p2 32768 1953521663 1953488896 931.5G Microsoft basic data
# 挂载该磁盘 到  /media/lilei/1T1
```

```shell
# ChatGPT
# 为了将分区 /dev/nvme1n1p2 挂载到 /media/lilei/1T1，你可以按照以下步骤进行：
```


```shell
# 创建挂载点（如果不存在）：
sudo mkdir -p /media/lilei/1T1
#使用 mount 命令挂载分区：
sudo mount /dev/nvme1n1p2 /media/lilei/1T1
#这样，分区 /dev/nvme1n1p2 就会被挂载到 /media/lilei/1T1。
```



#### qt

```shell
pyuic5 -o winqt_101.py winqt_101.ui 
```


```python
import sys,os
from PyQt5.QtWidgets import QApplication, QMainWindow
from PyQt5 import QtCore, QtGui, QtWidgets
import time
import threading
import time
sys.path.append(os.getcwd())
from qt_handle_02 import Ui_MainWindow

class MyMainForm(QMainWindow, Ui_MainWindow):
    def __init__(self, parent=None):
        super(MyMainForm, self).__init__(parent)
        self.setupUi(self)
if __name__ == "__main__":
    #固定的，PyQt5程序都需要QApplication对象。sys.argv是命令行参数列表，确保程序可以双击运行
    app = QApplication(sys.argv)
    #初始化
    myWin = MyMainForm()
    #将窗口控件显示在屏幕上
    myWin.show()
    #程序运行，sys.exit方法确保程序完整退出。
    sys.exit(app.exec_())

```

```shell
self.textBrowser_msg.insertPlainText('['+ str(datetime.datetime.now())+']\t关闭状态')
time.sleep(0.08)            # pyqt 发送过快会崩溃
```


#### 语音相关
查看设备
```shell
pactl list short sinks
```
切换设备
```shell
pactl set-default-sink <sink_name>
```
```shell
pactl set-default-sink alsa_output.pci-0000_00_1b.0.analog-stereo
```
加音量
```shell
amixer set Master 10%+
```


#### python3

```shell
-i https://pypi.tuna.tsinghua.edu.cn/simple
```

```
pip install -e .
```
```python
python -m pip install tensorboard
```

```python
import csv
from datetime import datetime
b = datetime.now().strftime('%Y%m%d%H%M%S')
with open('Epoch_train.csv', "a") as f:
    f.write(datetime.now().strftime('%Y%m%d%H%M%S') + '\n')
```


##### python3 多线程
```
import threading

def print_numbers(thread_name):
    for i in range(1, 600):
        print(f"{thread_name} 打印: {i}")

# 创建两个线程
thread1 = threading.Thread(target=print_numbers, args=("线程1",))
thread2 = threading.Thread(target=print_numbers, args=("线程2",))

# 启动线程
thread1.start()
thread2.start()

# 等待线程执行结束
thread1.join()
thread2.join()

print("所有线程执行完毕")
```



##### python3剪视频
```python3
# 将3.2s之后的视频保存为新视频
import subprocess

def clip_video(input_video_path, output_video_path, start_time):
    # 构造ffmpeg命令
    command = [
        'ffmpeg', 
        '-ss', str(start_time),  # 从start_time秒开始剪辑
        '-i', input_video_path,  # 输入文件路径
        '-c', 'copy',            # 直接复制编码（无重新编码）
        '-y',                    # 自动覆盖输出文件（如果存在）
        output_video_path        # 输出文件路径
    ]

    # 执行命令
    subprocess.run(command)

# 输入和输出文件路径
input_video = '/home/ubuntu/Videos/Screencast from 2025年02月24日 17时39分05秒.webm'
output_video = 'output_video.webm'

# 从第 3.2 秒开始剪辑
# clip_video(input_video, output_video, start_time=3.2)
clip_video(input_video, output_video, start_time=15)

print(f'视频剪辑完成！输出文件：{output_video}')
```

```shell
# 创建frames文件夹，将video.mp4 每一帧保存在该文件夹
mkdir frames
ffmpeg -i video.mp4 frames/frame_%05d.png
```





##### 直接安装whl
```shell
pip install xxx.whl
```

##### 新建环境
```shell
conda create -n your_env_name python=3.8
```
```shell
conda create -p /xxx/xxx/anaconda3/envs/env_name python=3.10.8
```
```shell
# 假设已有环境名为A，需要生成的环境名为B：
conda create -n B --clone A
```
```shell
CUDA_VISIBLE_DEVICES=1 python3 src/llm_task_pkg/llm_task_pkg/llm_glm2.py
```
pytorch

pytorch  官网
```shell
https://pytorch.org/
```
```shell
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

删除环境
```shell
conda env remove --name NLP-Tutorials-master
```

```shell
openai.api_key = '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'
```

```shell
import os
# # 设置你的文件夹路径
folder_path = '/home/ubuntu/Desktop/rl/learn_rl_simple/model_new/net_01_defussion/DL-Demos/dldemos/ddim'

# 使用os.listdir()列出文件夹中的所有项
entries = os.listdir(folder_path)

# 遍历文件夹中的项
for entry in entries:
    # print(entry)
    print( folder_path + '/' + entry    )
```
##### 引用模块 相对路径
```shell
import sys  ,os
# sys.path.append('需要引用模块的地址')
print(os.path.abspath(__file__))
print(os.getcwd())
print(os.path.dirname(__file__))
'''
/media/ubuntu/1T1/ubuntu_data/nlp/demo_26_Audio2Face/diff2lip/try.py
/media/ubuntu/1T1/ubuntu_data/nlp/demo_26_Audio2Face/diff2lip
/media/ubuntu/1T1/ubuntu_data/nlp/demo_26_Audio2Face/diff2lip
'''
```
```
import sys,os
sys.path.append(os.path.dirname(__file__))
```
```
import sys,os
sys.path.append(os.path.dirname(os.path.dirname(__file__)))
```
```
import sys,os
sys.path.append(os.path.dirname(os.path.dirname(os.path.dirname(__file__))))
```
```shell
# 键盘失灵
ibus-daemon -r -d -x
ibus-daemon -drx
```



```shell
import os
# 获取当前文件的上级目录路径
parent_dir_path = os.path.dirname(os.path.abspath(__file__))
print(parent_dir_path)
print(os.path.dirname(os.path.dirname(__file__)))
```

```python
import os
path = "/home/fu/Desktop/video/test.mp4"
filename_wo_ext = os.path.splitext(os.path.basename(path))[0]
print(filename_wo_ext)  # 输出: test
```



##### pytorch
扩充张量
```shell
.unsqueeze(0)
```

指定维度重复固定次数(第一个维度重复三次)
```shell
.repeat(3, 1, 1, 1)
```

##### blender

```
# 打开blender路径
cd ~/Desktop/ubuntu_data/program/blender-4.2.13-linux-x64/4.2/python/bin

# 确保 Blender 自带 Python 可用
./python3.11 -m ensurepip --upgrade

# 安装 PyYAML 模块
./python3.11 -m pip install pyyaml

# 检查是否安装成功
./python3.11 -m pip show pyyaml
```

#### ROS2指令
```
ros2 pkg create --build-type ament_python <package_name>
```

```
colcon build
```
```
colcon build --packages-select my_package
```






#### gitee&github


下载指定分支


```
git clone -b [分支名称] [git地址]
```
```
git clone -b voice_new_0326 https://gitee.com/usst-imi/voice_dialogue.git
```

```
ssh -T git@gitee.com 
# github 对应下面的
# ssh -T git@github.com 
 
git init

git add .

git commit -m "250515"

git remote add origin "git@gitee.com:zhugashvili/audio-feature.git"


git branch 250515
git checkout 250515

git push -u origin 250515
git pull --rebase origin 250515

git status

git add .gitignore

git commit -m "ignore"
git rebase --continue

git pull --rebase origin 250515

git push origin 250515

```





```
error
git push -u origin 250515
git@gitee.com: Permission denied (publickey).
fatal: Could not read from remote repository.

Please make sure you have the correct access rights
and the repository exists.
```
```
# 解决网站
https://blog.csdn.net/qq_52827181/article/details/120572994
```

