### 相关链接
相关教程 https://edu.gitee.com/usst-imi/projects/470245/repos/usst-imi/somo_voice/tree/centre_voice_xfnewmic

https://askubuntu.com/questions/1372720/why-pulseaudio-is-reported-as-not-running

https://blog.csdn.net/mbs520/article/details/113179224

https://www.jianshu.com/p/e04f6064dc5f

https://cloud.tencent.com/developer/article/1932876


### 配置

安装相关插件


```shell
sudo apt-get install libasound2-dev
sudo apt-get install sox
sudo apt-get install mplayer
sudo apt-get install sox libsox-fmt-all
sudo apt install pavucontrol
sudo apt-get install python-all-dev  portaudio19-dev
pip install pyaudio
pip3 install python-docx
pip install docx 
pip install jieba 
## 扬声器状态所需
sudo apt install pavucontrol
pip install playsound
pip install azure-cognitiveservices-speech
```

打开刚刚的somo_voice/src


```shell
robot@nuc12:~/upctrl/somo_voice/src$ ls
20230311-165407.png  aiui_readme  azure_ros  centre_voice  chatgpt_ros  readme.md  readme.txt  serros  somo_msgs  start.sh  vitsros  voiceinteraction  words2command
```

进入/src/aiui_readme安装cJSON


```shell
cd aiui_readme
sudo rm -r cJSON
apt  install cmake 
sudo apt-get install rar
sudo apt-get install unrar
sudo unrar x cJSON.rar
cd cJSON
sudo mkdir build
cd build/
sudo cmake ..
sudo make
sudo make install 
```

连接新麦克风到电脑,将串口ttyACM0 修改成 wheeltec_mic


```shell
cd aiui_readme
sudo chmod +x ttyacm0.sh
sudo bash ttyacm0.sh
sudo cp lib* /usr/lib
ls /dev/w*   # 查看是否有wheeltec_mic   有则更改成功
```


命令行调节音量（有桌面可以直接在桌面设置中设置输入输出设备）

```shell
amixer -D pulse sset Master 5%+        # +5%
amixer -D pulse sset Master unmute        # 解除静音
```

录音和播音，测试麦克风是否正确设置和打开

```shell
arecord -d 10 test.wav
#正在录音 WAVE 'test.wav' : Unsigned 8 bit, 频率8000Hz， Mono
aplay test.wav
#正在播放 WAVE 'test.wav' : Unsigned 8 bit, 频率8000Hz， Mono
```

如果音频播放只有sudo才能进行      （加权限解决）

解决办法（如果可以正常不加）

```shell
sudo pulseaudio --kill        # 终止 PulseAudio 音频服务器。关闭 PulseAudio，停止音频服务。
sudo pulseaudio --start        # 启动 PulseAudio 并使其提供音频服务。
sudo pacmd list-sinks        # 列出 PulseAudio 中可用的音频输出设备，也就是音频输出源。这个命令将显示所有已检测到的音频输出设备以及它们的详细信息
```

```shell
ls -l /dev/snd/
sudo chmod a+rw /dev/snd/*
```

针对全局配置： 在/etc目录下创建文件asound.conf   针对指定用户配置： 在用户目录下创建文件.asoundrc

```shell

sudo vim .asoundrc  # 或者  sudo vim /etc/asound.conf

```

写入


```shell
ctl.!default {
     type pulse
     sink "alsa_card.usb-iflytek_XFM-DP-V0.0.18_6c001440601406d1e50-01"
     source "alsa_card.usb-C-Media_Electronics_Inc._USB_Audio_Device-00"
 }
#讯飞麦克风 alsa_card.usb-iflytek_XFM-DP-V0.0.18_6c001440601406d1e50-01
#扬声器 alsa_card.usb-C-Media_Electronics_Inc._USB_Audio_Device-00
#纽曼一对麦克风 alsa_card.usb-SmartlinkTechnology_S6_20121120222017-00
```


//在libs文件下 ，对应id 对应 lib*文件

```shell
cd somo_voice/src/voiceinteraction/libs/x64
sudo cp lib* /usr/lib
```

根据aiui_readme/版本说明_老/新版.txt 安装所需插件，代码中账号本来是设置好的，这里可以先跳过，可以配置完之后再更改


编译


```shell
colcon build --packages-select somo_msgs
source install/setup.bash
colcon build
```

测试科大讯飞关键文件

```shell
ros2 run voiceinteraction aiui_new_mic     # 对着麦克风说 '小贝小贝'或者'你好瑞纳'尝试唤醒看看有没有反应
```



### 账号和科大讯飞配置文件更改
之前的配置已经可以实现基本问答，但是仍然需要申请自己的账号和配置文件以保证长期稳定运行

创建帐号
- https://aiui.xfyun.cn/ ## 帐号注册,应用创建
	- 在网页右上角(帐号位置)->进行实名制用于后续sdk 下载
	- 我的应用 -> 进入应用 -> 新建应用 -> 应用信息 appid  
	- humanoid_voice_base/src/humanoid_voice/awaken_node.py -> 230行 替换 appid 
	- humanoid_voice_base/src/humanoid_voice/iat_publish.py -> 161行 替换 appid
	- humanoid_voice_base/src/humanoid_voice/tts_subscribe.py -> 231行 替换 appid
- https://www.xfyun.cn/ ## 唤醒词语设置,sdk下载,对应文件替换
	- 点击控制台 -> 选择应用:自己创建的 -> 语音识别:语音唤醒 -> 唤醒此设置:小贝小贝 -> 提交 -> 语音唤醒SDK: Linux MSC 下载
	-> 选择平台:Linux -> AI能力:语音唤醒 -> 下载解压
	替换 lib 文件
		- 解压文件/libs/x64/libmsc.so 和 libw_ivw.so
		- humanoid_voice_base/libs/x64/libmsc.so 和 libw_ivw.so-> 替换   
		- 将下载的sdk文件的bin下面的msc全体文件粘贴替换 src/humanoid_voice_base/msc
		- 当前目录打开 terminal
			- sudo cp lib* /usr/lib
			// - sudo ldconfig















